using IRHL.MVC.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace IRHL.MVC.Service
{
    public class GameService
    {
        //public void AddGameToTeams(int gameID, int teamOneID, int teamTwoID)
        //{
        //    using (var ctx = new ApplicationDbContext())
        //    {
        //        var foundGameID = ctx.Games.Single(g => g.GameID == gameID);
        //        var foundTeamOneID = ctx.Games.Single(o => o.TeamOneID == teamOneID);
        //        var foundTeamTwoID = ctx.Games.Single(t => t.TeamTwoID == teamTwoID);
        //        foundGameID.ListOfTeams.Add(foundTeamOneID);
        //        foundGameID.ListOfTeams.Add(foundTeamTwoID);
        //        var testing = ctx.SaveChanges();
        //    }

        //    if (TeamOneGoals > TeamTwoGoals)
        //    {
        //        Team
        //    }
        //}

        //public bool AddGoalsToTeams(Game game, Team team)
        //{

        //}

    }
}